# Image sources and licences

The current rebuild uses no external photographic imagery. The technical hero visual is generated with HTML and CSS, and all brand marks are production SVG assets stored in `brand/volume-02/assets/`.

Before adding photography, record the creator, source URL, licence, download date, permitted use, attribution requirements and local optimised filename here.
